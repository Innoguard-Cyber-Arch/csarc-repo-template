{
  "schema_version": 1,
  "template_mode": "{{ project_mode }}",
  "language_profile": "{{ language }}",
  "modules": {
    "python": {{ "true" if language != "typescript" else "false" }},
    "typescript": {{ "true" if language != "python" else "false" }}
  }
}
