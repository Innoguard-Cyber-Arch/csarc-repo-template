# {{ project_name }}

## 專案概述

{{ project_description }}
公版成熟階段：beta。本段應持續說清楚專案解決的問題、主要使用者、輸出邊界與使用範圍。

## 技術與目錄

本 repo 宣告為 `{{ language }}`，記錄在 `.csarc/profile.json`。`scripts/detect-language-profile --suggest` 會依 `pyproject.toml` 與 `package.json` 判斷實際模組；`scripts/verify` 會阻擋宣告與檔案不一致，避免只加一個設定檔卻漏掉整套門禁。

`.gitignore` 固定排除 macOS、Windows、Linux 與常見 IDE 的本機雜訊，並依這個 profile 只加入{% if language != "typescript" %} Python{% endif %}{% if language == "python-typescript" %}、{% endif %}{% if language != "python" %} TypeScript{% endif %} 產物規則。VS Code 的 `settings.json`、`tasks.json`、`launch.json`、`extensions.json` 仍可提交，讓真正的團隊設定與個人暫存檔分開。

{% if language != "typescript" %}
Python 以 [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html) 為基準；Ruff 會檢查 80 字元行寬、import、命名與 Google docstring，mypy 負責 strict 型別檢查。

{% if python_support_mode == "latest" %}本 repo 採「最新穩定版」模式，目前只承諾 Python 3.14：`requires-python`、Ruff、mypy、本機 `.python-version` 與 CI 都以 3.14 為準。公版日後更新最新穩定版時，Copier 會先形成可審查的 PR 差異。{% else %}本 repo 採「最低版本」模式，宣告支援 Python {{ python_min_version }} 以上；Ruff 與 mypy 以 {{ python_min_version }} 為語法下限，CI 同時驗證 {{ python_min_version }} 與目前最新穩定版 3.14。本機開發仍使用 `.python-version` 固定的 3.14。{% endif %}
{% endif %}

{% if language != "python" %}
TypeScript 以 Node 24 Active LTS、pnpm 11 為基線；Biome 負責排版與 lint、TypeScript strict 模式負責型別、Vitest 負責測試與 {{ coverage_threshold }}% coverage。`pnpm-workspace.yaml` 會以嚴格模式拒絕發布未滿三天的新版本，`pnpm-lock.yaml` 則記錄下載內容的 integrity hash。
{% endif %}

公版更新會對 `.github/`、`policies/`、`scripts/verify`、`README.md`、`AGENTS.md` 與 `CLAUDE.md` 提出可審查差異；語言設定由公版和專案共同維護。{% if language != "typescript" %}`src/`、`tests/`、{% endif %}{% if language != "python" %}`typescript/src/`、`typescript/tests/`、{% endif %}`docs/specs/` 初次建立後屬於專案，Copier 設定為不改寫既有內容。

## 快速開始

共同前置工具是 Git、GitHub CLI 與 uv；{% if language != "python" %}本 repo 另需 Node 24+ 與 pnpm 11。{% endif %}取得 repo 後，先用共同入口確認環境與成品都可用：

```bash
git clone <repository-url>
cd {{ project_slug }}
./scripts/verify
```

{% if project_mode == "existing" %}
這是既有專案導入：Copier 已保留原本的{% if language != "typescript" %} `pyproject.toml`{% endif %}{% if language == "python-typescript" %} 與{% endif %}{% if language != "python" %} `package.json`{% endif %}，不會自動合併產品相依、版本或 build backend。第一次驗證前，先人工合併公版的 dev 相依與工具區段{% if language != "typescript" %}，確認 `[project].version` 與 `src/{{ package_name }}/__init__.py` 的 `__version__` 一致，再執行 `uv lock` 與 `uv run ruff format .`{% endif %}{% if language == "python-typescript" %}；{% endif %}{% if language != "python" %}再執行 `pnpm install --lockfile-only --ignore-scripts` 與 `pnpm exec biome format --write .`{% endif %}。`coverage_mode=diff` 預設比較 `origin/main`；若尚未有該 ref，先推送遠端，或以 `DIFF_COVER_COMPARE_BRANCH=<existing-ref>` 指定真正存在的基準。
{% endif %}

專案維護者應在本節接著補上最短的啟動或呼叫範例；README 不應只告訴讀者如何跑檢查，卻沒說產品怎麼使用。

## AI 工作契約

`AGENTS.md` 告訴 AI 哪些來源可信、工作要怎麼驗證、哪些操作不可做，以及審查時要注意什麼；它不重複 README 或 linter 設定。語言迭代命令會依本 repo 的 profile 產生，最後仍統一執行 `./scripts/verify`。只有子目錄真的有不同命令、owner 或安全邊界時才增加就近 `AGENTS.md`；`CLAUDE.md` 只匯入根檔，避免兩套規則漂移。

## 開發與驗證

1. 用 Work item 開一張可獨立驗證的 Issue。
2. 在分支完成一個結果；AI 可以協助寫程式，但不能自行合併。
3. 執行 `./scripts/verify`。它會先核對語言宣告，接著執行{% if language != "typescript" %} Ruff、mypy、pytest、{{ coverage_threshold }}% {% if coverage_mode == "global" %}全專案{% else %}變更行{% endif %}覆蓋率、wheel 隔離安裝與 Python 相依下界驗證{% endif %}{% if language == "python-typescript" %}，以及{% endif %}{% if language != "python" %} Biome、TypeScript strict、Vitest {{ coverage_threshold }}% coverage、npm tarball 隔離安裝{% endif %}，最後檢查規格、政策 JSON、機密字串與 lockfile。
4. 開 PR，等 CI、必要審查與 CODEOWNER 同意後再合併。

PR 標題使用英文 `type(scope): summary`，內文可以使用中文。可用的 type 是 `feat`、`fix`、`docs`、`refactor`、`test`、`build`、`ci`、`chore`、`revert`；scope 可省略，若有則用小寫。破壞性變更在冒號前加 `!`，例如 `feat(api)!: remove v1 endpoint`。

## 發布與維運

PR squash 合併到 `main` 後，release-please 會依標題累積下一版內容並開一張 Release PR。`fix` 升修補版、`feat` 升次版，`!` 升大版；其他 type 依用途整理 CHANGELOG。審查 Release PR 內的{% if language != "typescript" %} `pyproject.toml`、`uv.lock`、`src/{{ package_name }}/__init__.py`、{% endif %}{% if language != "python" %} `package.json`、{% endif %}`CHANGELOG.md`，合併後才會建立 `v*` tag；`release.yml` 隨即產生{% if language != "typescript" %} Python wheel／sdist{% endif %}{% if language == "python-typescript" %}與{% endif %}{% if language != "python" %} npm tarball{% endif %}、SHA-256 與 SBOM。

公版目前只建立可追溯的發布成品，沒有通用部署流程。專案若加入環境或服務，必須在本節補上部署入口、健康檢查、復原方式與 runbook 位置。

## 設定與密鑰

Organization 管理者先到 **Settings → Developer settings → GitHub Apps** 建立不使用 webhook 的專用 App，授予 Contents、Pull requests 讀寫權限，安裝到此 repo，再於 App 頁面產生 private key。把 App ID 與 PEM 寫入 GitHub：

```bash
gh variable set CSARC_VERSION_BOT_APP_ID --body '<app-id>'
gh secret set CSARC_VERSION_BOT_PRIVATE_KEY < ./private-key.pem
```

workflow 每次執行才由 private key 產生短效 token，不需人工保存 token；完成設定後，刪除本機 PEM 或移入核准的密鑰管理工具。不要把 App key 或 token 寫進 `.env`：Actions 使用 GitHub Secrets／Variables，正式環境使用密鑰管理或 OIDC；只有本機程式確實需要環境變數時，才使用已被忽略的 `.env`，並只提交去除敏感值的 `.env.example`。Python 與 uv 不會自動載入 `.env`。

產品若有其他設定，應在本節列出名稱、用途、是否必填與取得位置；只記錄範例值，不記錄 token、密碼或私鑰。

## 規格與 Issue

從 `docs/specs/SPEC-001-example.md` 複製一份規格。五個必要段落是 Problem、Outcome、Acceptance criteria、Out of scope、Verification；Priority 與 Expected size 是另外的規劃欄位。PR 會驗證格式，合併到 `main` 後會建立或更新相同 ID 的 GitHub Issue。

## 公版更新

Dependabot 會依 profile 更新{% if language != "typescript" %} uv 的 `pyproject.toml`／`uv.lock`{% endif %}{% if language == "python-typescript" %}與{% endif %}{% if language != "python" %} npm 的 `package.json`／`pnpm-lock.yaml`{% endif %}，一般新版本等待三天再提 PR；OSV 對已公開漏洞立即通知。更新 PR 仍要執行 `./scripts/verify`：{% if language != "typescript" %}`uv sync --locked` 會比對 Python 成品 SHA-256；{% endif %}{% if language != "python" %}`pnpm install --frozen-lockfile` 會比對 npm integrity hash，resolver 也會拒絕發布未滿三天的版本；{% endif %}版本號方便閱讀，真正執行內容以 lockfile 雜湊與完整 commit SHA 為準。

公版更新從分支執行，不可直接改 `main`：

```bash
git switch -c chore/update-repo-template
uvx --from copier copier update --trust --vcs-ref <reviewed-full-commit-sha>
./scripts/verify
```

## 負責人與支援

程式審查負責人由 `.github/CODEOWNERS` 指向 `{{ code_owner }}`。一般問題、功能需求與可重現錯誤使用 GitHub Issue；CI 失敗先看該 job 的第一個實際錯誤。疑似資安事件或含敏感資料的問題不可貼到公開 Issue，應走組織核准的內部通報管道；正式上線前必須在此補上具體聯絡入口。

## GitHub 設定

`policies/` 是可審查的 GitHub 設定。一般本機驗證不需要 GitHub token；只有套用設定、同步 Issue 或測試 release workflow 時才需要。管理者先執行 `gh auth status`，確認登入者具有 repo 管理權與 Organization team 讀取權，再執行 `./scripts/apply-repository-settings.sh plan`，確認後才執行 `apply`。腳本也會確認 CODEOWNERS team 真實存在。它會設定 squash-only、Actions 預設唯讀、labels 與 `main` Ruleset；團隊權限仍在 Organization 管理。
