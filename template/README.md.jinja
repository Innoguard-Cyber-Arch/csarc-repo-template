{% set has_python = "python" in languages -%}
{% set has_typescript = "typescript" in languages -%}
{% set has_rust = "rust" in languages -%}
{% set has_container = (container_mode | default("none")) != "none" -%}
# {{ project_name }}

> [!IMPORTANT]
> AI 工作規範以 [`AGENTS.md`](AGENTS.md) 為唯一來源；只有 `.github/workflows/` 內的流程會執行，審查與合併資格以[08 規則治理](docs/index.html#governance)為準。

<!-- csarc-project-description:start -->
{{ project_description }}
<!-- csarc-project-description:end -->

[開啟專案內部網站](docs/index.html)

## 目錄

- [專案概述](#專案概述)
- [快速開始](#快速開始)
- [技術與目錄](#技術與目錄)
- [開發與驗證](#開發與驗證)
- [設定與密鑰](#設定與密鑰)
- [發布與維運](#發布與維運)
- [公版更新](#公版更新)
- [負責人與支援](#負責人與支援)

## 專案概述

本 repo 使用 CSARC 公版，程式語言為 {% if languages %}`{{ languages | join('`、`') }}`{% else %}僅共通 CI/CD{% endif %}。完整團隊流程放在內部網站，不在 README 重複。

## 快速開始

共同需求是 Git、GitHub CLI、uv；{% if has_typescript %}本案另需 Node 24+ 與 pnpm 11。{% endif %}{% if has_rust %}本案另需 Rust 1.98。{% endif %}

請從實際 Git root 開啟 Codex／agent workspace；從 repo 上層開啟時，子目錄的 `AGENTS.md` 不一定會自動載入。開始前先在工作目錄執行 `test "$(git rev-parse --show-toplevel)" = "$(pwd -P)"`，失敗就切換到輸出的 Git root，不要複製另一份指引到父目錄。

```bash
git clone {{ repository_url }}
cd {{ project_slug }}
./scripts/verify
./scripts/apply-repository-settings.sh plan
```

產品最短可成功的啟動或呼叫方式：

<!-- csarc-project-run-command:start -->
```bash
{{ project_run_command }}
```
<!-- csarc-project-run-command:end -->

`project_run_command` 只描述產品啟動方式，不會被當成驗證 hook。hook 不得解析成或重新進入 canonical `scripts/verify`；update 會先在暫存 clone 驗證再寫入。{% if project_verification_hook | default('', true) %}完整驗證會另外且只執行一次 `{{ project_verification_hook }}`。{% elif project_mode == "existing" %}未指定顯式 hook；若 project-owned `scripts/verify-product` 存在且可執行，會作為相容 fallback 執行一次。{% endif %}

{% if project_mode == "existing" %}> 既有案導入：Copier 已保留原本的 manifest。先人工合併 dev 相依、版本與工具設定，再重建 lockfile；`coverage_mode=diff` 預設比較 `origin/main`。{% endif %}
{% if branch_strategy == "delivery" %}> 本案選用 delivery 模式：`main` 是唯一永久 branch；Milestone 使用短命的 `dev/m<編號>-<簡稱>`，一般孤立 Issue 從最新 `main` 建立 topic branch 並直接以 PR 回到 `main`。確實需要獨立 soak／canary 的 Issue 才使用一次性的 `dev/i<Issue 編號>-<簡稱>` promotion。{% else %}> 本案使用 main 模式，短分支通過 PR 後直接合併到 `main`。{% endif %}
{% if branch_strategy == "delivery" %}
```mermaid
flowchart LR
  A["Milestone A Issues"] --> MA["dev/m7-delivery"]
  B["Milestone B Issues"] --> MB["dev/m8-auth"]
  S["一般孤立 Issues"] --> MAIN
  I["需獨立 canary 的 Issue #42"] --> DI["dev/i42-canary"]
  H["緊急 fix/* + hotfix"] --> MAIN["main"]
  MA -->|promotion: full + canary| MAIN
  MB -->|promotion: full + canary| MAIN
  DI -->|單獨 promotion| MAIN
  MAIN -. "reviewed sync PR" .-> MA
  MAIN -. "reviewed sync PR" .-> MB
  MAIN -. "reviewed sync PR" .-> DI
```

`main` 前進不會讓 ordinary Milestone PR stale，也不會 fan out 同步。各 Milestone 到 final promotion 前才建立一張 reviewed `sync/main-to-m*` PR；只有 owner 明列 dependency 時可提前同步自己的 branch。`dev/i*` canary 也必須在 promotion 前包含當時最新 `main`。
{% endif %}

## 技術與目錄

| 範圍 | 位置／工具 |
| --- | --- |
| 共用 | `.github/`、`policies/`、`docs/adr/`、`docs/specs/`、`scripts/verify` |
{% if has_python %}| Python | `src/`、`tests/`、uv、Ruff、ty、pytest；CI 驗證 {% if python_support_mode == "latest" %}3.14.0 與最新 3.14.x{% else %}{{ python_min_version }}.0，以及 {{ python_min_version }}～3.14 每個 feature release 的最新 patch{% endif %} |
{% endif %}{% if has_rust %}| Rust | `src/`、Cargo、rustfmt、Clippy、內建測試；Rust 1.98 |
{% endif %}{% if has_typescript %}| TypeScript | `typescript/`、pnpm 11、Biome、strict TypeScript、Vitest；Node 24+ |
{% endif %}{% if has_container %}| 容器 | `{{ containerfile_path }}`、Buildx cache、smoke test 與 Trivy；build context 固定為 repo root |
{% endif %}| 內部網站 | `site/` 公版來源；`docs/site-content.js` 與 `docs/site-theme.css` 專案內容；`docs/index.html` portable bundle |

公版設定記在 `.csarc/config.yml`；建立或導入時選擇語言，`scripts/detect-language-profile --suggest` 會檢查實際檔案是否一致。請透過 `csarc update --data` 變更公版選項，不要直接修改設定檔。
{% if has_python %}Python minimum 選項目前從 3.12 起，未宣告支援 3.11；所選 feature release 的 `.0` 就是 `requires-python` 的實際下界，CI 不會只測較新的 patch。{% endif %}

本模板的 Durable Project Memory 同時支援 Spec-Driven Development（SDD）、Architecture Decision Records（ADR）、Test-Driven Development（TDD）的回歸證據與 Behavior-Driven Development（BDD）的必要行為情境；完整分工見 [`docs/README.md`](docs/README.md)。

## 開發與驗證

工作模型是「SDD → Feature parent → Task／Bug subissues → 各自 PR」。只有可獨立負責、測試或交付的工作才拆 subissue；dependency 只表示真實 blocked／blocking。Milestone 另作有 due date 的 delivery／release bucket，掛 leaf Issues 與其 PR，不掛 Feature parent。GitHub Projects 預設關閉。建立 work item 前，agent 會以具體關鍵字限量搜尋 open／closed Issues，閱讀候選內文、comments 與 linked PR，先向使用者摘要；若已獲准直接建立，則在補充或 Milestone `References` 記錄每項決策是沿用、取代或駁回及理由。最後一張 open Issue 關閉且所有 acceptance checkbox 已確認時，workflow 才會關閉 Milestone；Issue reopen、open Issue 掛入或 criterion 取消勾選時則重開。

1. 簡單工作先開「開發工作」Issue；標題以 12–80 個英文 ASCII 字元及至少三個詞直接描述成果，例如 `Add dependency policy checks`。Issue 不使用 PR 的 Conventional Commit 格式。
2. 從 Feature、Task、Bug 或 Documentation 表單建立工作；表單直接設定原生 `Feature`、`Task`、`Bug`、`Task` Type，並以 `enhancement`、`bug` 或 `documentation` Label 保留跨 Issue／PR 分類。UI 建立者自行指派自己，agent／CLI 建立時使用 `--assignee @me`；`duplicate` 是既有 Issue 的結案處置，不是建立類型。
3. {% if branch_strategy == "delivery" %}Milestone Issue 從對應 `dev/m<Milestone 編號>-<簡稱>` 建立工作分支；沒有 Milestone 時從最新 `main` 建立。只有 Issue 記錄獨立 soak／canary 理由時，才直接使用一次性的 `dev/i<Issue 編號>-<簡稱>` 工作與 promotion。{% else %}從 `{{ branch_strategy }}` 或最終 PR 回 `{{ branch_strategy }}` 的未合併工作分支建立。{% endif %}一般工作分支使用 `gh issue develop <issue> --base <base> --name type/<issue-number>-short-slug` 建立，確保 GitHub Development 關聯；若新增需求超出 Issue 的完成條件，另開 Issue 與分支。若後來確認重複，連結 canonical Issue 並用 GitHub 的 Duplicate 原生原因關閉；純 triage 不改檔案，可不開分支與 PR。
4. 執行 `./scripts/verify`；AI 遵守 `AGENTS.md`，審查、合併資格與 Alpha self-merge 例外以[08 規則治理](docs/index.html#governance)為準。
5. PR 指向{% if branch_strategy == "delivery" %}Issue 所屬的 `dev/m*`，或在沒有 Milestone 時指向 `main`{% else %} `{{ branch_strategy }}`{% endif %}，也可指向 stack 中的直接上游分支；整條 open PR 鏈必須最終回到正確 base。使用 `Closes #<issue-number>` 前，PR 與 referenced Issue 不得留有未勾選 task；風險或回退放在選填補充。PR 沒有原生 Issue Type／Issue Form，只使用 Markdown template；作者自行指派自己，並讓 Milestone 與恰好一個 `bug`、`enhancement` 或 `documentation` 分類 Label 對齊 linked Issue。{% if branch_strategy == "delivery" %}工作 PR 合併進 `dev/m*` 後，Action 會重新核對同號 Issue、精確來源 SHA、目的分支與 Milestone 再關單；沒有 Milestone 的一般工作與 Hotfix 直接進 `main`，沿用 GitHub 原生關單。{% endif %}
6. 合併前依 08 規則治理檢查目前有效的 review、required checks、Ruleset 與明確授權；README 不另外定義 self-merge 例外。{% if branch_strategy == "delivery" %}Milestone 建立時同時建立一張 `promotion` Issue；完成後用 `dev/m* → main` PR 關閉它。一般孤立工作直接回 `main`；`dev/i*` 的同號 Issue 本身就是 promotion Issue，合併後刪除該暫時 branch。Standalone `fix/*` 只有加上 `hotfix` label 才是緊急 release boundary。{% endif %}

多個可寫 agents 平行執行時，一項任務使用一個 branch 與一個獨立 worktree，且只平行處理範圍互不依賴的工作；開始前先辨識目前是否已在 agent 平台管理的 worktree，不強制讓同一 branch 出現在多個 worktrees，也不自行刪除其他工具建立或含未提交變更的 worktree。原生 [`git worktree`](https://git-scm.com/docs/git-worktree) 是可攜基線；[Worktrunk](https://github.com/max-sixty/worktrunk) 可選擇簡化建立、切換與清理，但屬於本機／agent orchestration 輔助工具，不是本模板或生成 repo 的必要相依。worktree 只隔離工作目錄，整合仍以 PR、CI、人工審查及合併後完整驗證為準。

GitHub Actions 看不到 agent host 上的本機 worktrees，因此建立 worktree 的 agent 也負責在 PR 合併後回收：先離開該 worktree 並更新 integration ref，再從另一個 checkout 執行 `./scripts/cleanup-worktrees --apply --worktree <path> origin/<pull-request-base>`。腳本預設只列出候選項目，且只接受有 GitHub merged PR 證據、已進入 integration ref、乾淨且未鎖定的 branch worktree；維護者要清理全 repo 時，先執行不含 `--apply` 的 dry run，再省略 `--worktree` 套用全部候選項目。

`./scripts/verify` 是本機與 CI 共用的完整入口。現行 `.github/workflows/ci.yml` 只有一個 `verify` job，依變更選擇 docs／fast／full，再呼叫 repo-local 程式；一般 PR 不會為 fast、full、安全與 aggregate 各啟動一個 runner。promotion、hotfix、release recovery、merge queue 與手動執行採 full，單一 job timeout 為 30 分鐘。詳細分級與目前封存邊界見 [`docs/ci-policy.md`](docs/ci-policy.md)。

{% if branch_strategy == "delivery" %}專用的 promotion、release、OSV、Zizmor、remote governance、deployment 與 schedule workflows 目前仍封存。這些能力要等各自 Journey 重新核准後才恢復；文件中的目標政策不代表 Action 已啟用。{% endif %}

### Actions 額度耗盡的一次性驗證

只有 GitHub Actions 的 zero-step billing block 被機械式確認、且本機驗證通過時，才可能使用本機 fallback；runner 註記本身不構成證據。一般 Issue PR 可留一則說明留言；direct-main standalone 仍是 human-only merge{% if branch_strategy == "delivery" %}，Promotion 到 `main` 則維持 human attestation/authorization 雙方確認，另須綁定 candidate tree、合併後核對 tree identity，且本機證據不可用於 release{% endif %}。完整流程只有一份，見 [`docs/ci-policy.md`](docs/ci-policy.md#actions-額度-fallback)。

`./scripts/scan-secrets` 會在已有 commit 時掃描完整可達 Git 歷史，並一律另掃目前工作樹；尚未 `git init` 的新專案仍可掃描工作樹。大型 repo 若已明確接受縮小歷史範圍，可傳入例如 `--log-opts='--since=2026-01-01'`，預設仍掃完整歷史。

{% if enable_codeql | default(false) %}此專案已產生 CodeQL workflow，分析已選取的 Python／TypeScript 模組；它補足跨函式資料流等 SAST 檢查，但仍可能有誤報與漏報，結果需人工 triage。{% else %}此專案未啟用 SAST。語言 lint 與 OSV 分別處理程式品質和已知相依漏洞，不能替代 SAST；public repo 可重新產生並啟用 `enable_codeql`，private／internal repo 則需先確認 GitHub Code Security 授權，或由專案選用核准的替代工具。{% endif %}

`./scripts/verify-template.sh`、root 專用升版／同步工具與 template release workflows 只屬於中央模板 repo，不會下發到本專案；本專案的本機與 CI 驗證入口都是 `./scripts/verify`。

{% if not languages %}目前驗證 spec、README、JSON、workflow 與機密字串；沒有語言模組，因此不執行語言測試或打包。
{% else %}目前驗證包含{% if has_python %} Ruff、ty、pytest、{{ coverage_threshold }}% coverage、wheel 與相依下界{% endif %}{% if has_python and (has_rust or has_typescript) %}，以及{% endif %}{% if has_rust %} rustfmt、Clippy、測試、release build 與 Cargo package{% endif %}{% if has_rust and has_typescript %}，以及{% endif %}{% if has_typescript %} Biome、TypeScript、Vitest {{ coverage_threshold }}% coverage 與 npm tarball{% endif %}，最後再檢查 spec、機密字串與 lockfile。

{% if has_python or has_typescript %}
Coverage 是找出未測程式碼的訊號，不等於測試品質，也不取代風險分析與人工 review。新案以 global coverage 建立基線；既有案先用 diff coverage 約束本次變更。只有缺陷紀錄、關鍵模組或風險改變時才調整 {{ coverage_threshold }}% 門檻，不要只為提高百分比加入沒有驗證行為的測試。
{% endif %}
{% endif %}

{% if has_container %}容器模式是 `{{ container_mode }}`：非 docs PR 會用 `{{ containerfile_path }}` 建置 image，執行設定的 `$IMAGE` smoke command，再以 Trivy 阻擋 HIGH／CRITICAL 漏洞。這個 job 不 push image，也沒有 registry write permission；本機 `./scripts/verify` 只驗證設定與 Containerfile 存在，實際 image build 由 GitHub-hosted runner 執行。
{% endif %}

## 設定與密鑰

GitHub 從模板建立 repo 或 Copier 導入只會複製檔案，不會複製 repository settings。有管理權且希望啟用較強門禁時，可在推送遠端後執行 `./scripts/apply-repository-settings.sh plan`，確認後執行 `apply`，最後以 `check` 唯讀比對 CODEOWNERS、repository、Actions、政策標籤與有效 Ruleset；這不是 portable release baseline 的必要步驟。可修正差異會失敗，Free private Ruleset、組織政策限制或 PR 的內建 `GITHUB_TOKEN` 看不到管理欄位時，則明確標為 `DEGRADED` 並保留具體差異。需要驗證全部管理欄位時，由管理員在可信任的 checkout 以具 repository Administration read 權限的 `gh` 憑證執行；不要把管理型 token 暴露給未受信任的 PR 程式碼。尚未設定 Git remote 時，明確指定 `GH_REPO=owner/repo`。標籤預設採 additive 更新並保留自訂項目；只有明確傳入 `--prune-labels` 才會刪除政策外標籤。Copier 不在本機建檔階段猜測遠端方案。

| GitHub 方案與可見性 | `apply` 結果 | `check`／PR／CI/CD 行為 |
| --- | --- | --- |
| Free＋public | 透過 REST 套用並啟用 Ruleset | 驗證 `main` 的有效規則；缺少或不符即失敗 |
| Free organization＋private | 套用基本設定，並把期望 Ruleset 保留在 `policies/rulesets.json`；公開 API 無法建立 Ruleset | workflow 從設定名單輪派一位個別 reviewer，並以 `DEGRADED` 警告未強制；team request、紅燈或未核准都不能成為 merge gate |
| Pro 個人帳號＋private | 套用並啟用 Ruleset | 與 Free public 相同 |
| Team／Enterprise organization＋private | 確認 CODEOWNERS team 後套用並啟用 Ruleset | 必要審查、CODEOWNER 與 status checks 成為 merge gate；不符政策時 fail-closed |

`policies/rulesets.json` 是所有方案共用的期望狀態。設定腳本在所有方案都透過 repository teams API 驗證 PR 內設定的 team 存在且對 repo 有 write access；這項檢查與是否能強制 Ruleset 分開，也不依賴預設分支尚未更新的 CODEOWNERS。`.github/workflows/governance-comment.yml` 另在非 draft PR 開啟、重開或轉為 ready 時，從 `.github/REVIEWERS` 排除作者後輪派一位個別 reviewer，並用短效 `GITHUB_TOKEN` 提出 request，不需 PAT 或 GitHub App。Copier 的 `reviewers` 可用逗號設定一到多位候選人。Free private 不支援 team review request 或多位同時 reviewer；GitHub Team 以上才提供這些能力。Free organization private repo 的設定頁雖提供 Ruleset 編輯器，但 GitHub REST 與 GraphQL 對建立／更新（包含 `disabled`）都回覆需升級或公開；因此 `gh` 無法自動預存。管理員可選擇在 Web UI 人工建立 disabled Ruleset，`check` 會將它顯示為 `STAGED`，但仍只代表已儲存，不代表 `main` 受保護。repo 改成 public 或組織升級後，請重新執行 `plan`／`apply`／`check`，由腳本套用 `active` 政策並驗證有效規則。腳本不會自行變更可見性。

{% if languages %}{% if enable_release_attestations | default(false) %}此專案會為 release distributions 產生 artifact provenance 與 SBOM attestation；這需要 public repo，或支援 artifact attestations 的 GitHub Enterprise Cloud private／internal repo。{% if enable_pypi_publishing | default(false) or enable_npm_publishing | default(false) %}外部 registry 發布會先以 `gh attestation verify` 強制比對 repository、tag ref、artifact digest 與 signer workflow；驗證失敗就不發布。{% else %}目前沒有自動消費或部署路徑，因此只產生 attestation，不宣稱已在消費端強制驗證。{% endif %}{% else %}Artifact attestation 未啟用，適合目前不支援該功能的 private repo；release 仍會附上 `SHA256SUMS` 與 SBOM，可用 checksum 驗證，但沒有 provenance 強制門禁。{% endif %}{% endif %}

release workflow 使用 Actions 內建 `GITHUB_TOKEN`，不需要設定 GitHub App 或 PAT。`csarc init`／`adopt`／`update` 在 GitHub origin 與 API 可讀時顯示 capability preflight；每次 workflow 仍重測 Actions PR、contents、Release 與 dispatch 能力。四項都確認可用時使用 release-please；PR 被禁止或無法確認但其餘交付能力完整時，從最新整合分支 commit 配置版本並交付；否則只驗證並上傳 machine-readable capability artifact。`plan`／`apply`／`check` 是有管理權時啟用較強門禁的選配，不會阻止 runtime 自適應。Actions 憑證放 GitHub Secrets／Variables；本機 runtime 才使用未提交的 `.env`。請在這裡列出產品設定的名稱、用途與取得位置，但不要填入真實值。

同一個唯讀 preflight 也會依 personal／organization owner、repo admin 與 organization owner 狀態，把選配 Renovate App 標成 `available`、`request-owner` 或 `fallback`。`available` 只提供 [安裝入口](https://github.com/apps/renovate/installations/new)，仍須在 GitHub 同意權限並只選本 repo；organization member 即使有 repo admin，仍需 organization owner 核准。未知權限、API 失敗或無 admin 時不猜測，直接保留 Dependabot 與既有 CI/CD。CLI 不會靜默安裝 App，也不要求長效廣域 PAT。新 repo 尚未有 origin 時，先設定 `GH_REPO=owner/repo` 可讓 `csarc init` 檢查預定目標；推送後也能執行 `python scripts/release_policy.py preflight --repo owner/repo` 重查。

## 發布與維運

PR 只顯示升版意圖：`fix(scope)` 是 patch、`feat(scope)` 是 minor、`!` 是 major，其餘是 no-release；不在 PR 階段預約確切版本。進入整合分支後才以實際 commit／tag 順序配置版本，過期或亂序 run 會停止。{% if branch_strategy == "delivery" %}因 repo 採 squash merge，promotion PR 的英文標題必須代表納入 PR 中最高的 SemVer 等級；Milestone 原則上在完成時 promotion 一次，只有後續驗收依賴同一 Milestone 的 immutable Release 才使用受約束的 checkpoint promotion。Standalone 與 bot PR 直接進 `main` 後只留下 release intent；`dev/i*` canary 與 hotfix 才立即形成獨立 release boundary。{% endif %}release-please 或 direct mode 建立 release 後，只能帶著已核對的 release-source run ID 明確 dispatch 成品 workflow；direct mode 只接受已由人工 PR 寫入版本與 CHANGELOG 的最新整合分支，否則停在 verification-only。任意 `v*` tag push 不會啟動發布；artifact workflow 會核對 promotion evidence 與 exact tag 後建置，以固定 Syft 版本產生 SPDX JSON SBOM，綁定 digest／manifest／provenance，再驗證 immutable Release；不重跑完整 runtime CI，也不要求 Syft 輸出 byte-identical。{% if not languages %}版本記在 `version.txt`；tag 發布來源封存檔與對應 SPDX SBOM，不假裝有語言套件。{% else %}核准的 `v*` tag 會產生{% if has_python %} wheel／sdist{% endif %}{% if has_python and has_typescript %}與{% endif %}{% if has_typescript %} npm tarball{% endif %}{% if (has_python or has_typescript) and has_rust %}與{% endif %}{% if has_rust %} Cargo package（不發布 crates.io）{% endif %}、來源封存檔、SHA-256、SPDX SBOM 與來源 metadata，並附加到對應 GitHub Release。下載後執行 `gh release download <tag>`，再於下載目錄執行 `shasum -a 256 -c SHA256SUMS`；{% if enable_release_attestations | default(false) %}另以 `gh attestation verify <artifact> --repo <owner/repo> --signer-workflow <owner/repo>/.github/workflows/release.yml --source-ref refs/tags/<tag>` 驗證 provenance。{% endif %}{% endif %}{% if (container_mode | default("none")) == "verify" %}容器只保留 CI 驗證，不會發布 registry。{% elif (container_mode | default("none")) == "ghcr" %}容器 release job 只接受同一條已驗證來源，保存已 smoke／scan 的 image bytes，發布版本與 commit SHA tag 至 GHCR，附加 provenance／SPDX SBOM 後以 digest 重跑 smoke test；只有發布 job 取得 `packages: write`。{% endif %}目前沒有通用 runtime 部署流程；批次與追溯規則見 [`docs/ci-policy.md`](docs/ci-policy.md)。

| Runtime 實測政策 | 模式與行為 | 保證、限制與 fallback |
| --- | --- | --- |
| PR、contents、Release、dispatch 都是 `allowed` | **Release PR：**release-please 維護可審查的版本／changelog PR；合併後明確 dispatch 成品 workflow | 保留最強的人類審查與來源 metadata 同步；必要能力漂移就不再選此模式 |
| PR 是 `blocked`／`unknown`，其餘三項都是 `allowed` | **Direct：**只為已版本化且含 CHANGELOG 的最新整合分支配置 tag／draft release | 亂序 run 成為 no-op；缺少版本 commit 時轉為 verification-only |
| contents、Release 或 dispatch 任一不是 `allowed` | **Verification only：**保留驗證與 capability artifact，不建立 tag 或 release | 不把不確定權限當成功；政策恢復後由後續 run 重新計算 |

GitHub Release 是共同基線；外部 registry 只會依本案 profile 與下列明確選項啟用，不是導入必要條件。
{% if enable_pypi_publishing | default(false) %}
PyPI 發布使用 environment `{{ pypi_environment }}` 與 OIDC，不需要 API token。Package owner 必須先在 PyPI 建立 project 或 pending publisher，將 organization、repository、workflow `release.yml` 與 environment 完整對應到本 repo；environment 建議要求人工核准。
{% endif %}{% if enable_npm_publishing | default(false) %}
npm 發布使用 environment `{{ npm_environment }}` 與 OIDC，不需要 `NPM_TOKEN`。既有 package owner 必須先在 npm package Settings 建立 trusted publisher，並精確填入 organization、repository、workflow `release.yml` 與 environment；首次 package ownership 依組織核准流程建立。npm trusted publishing 需要 GitHub-hosted runner、Node 22.14+ 與 npm 11.5.1+，本 workflow 使用 Node 24。
{% endif %}

若本案是長駐服務，請補上部署入口、健康檢查、復原方法與 runbook。

## 公版更新

先用 `gh release list --repo Innoguard-Cyber-Arch/csarc-repo-template` 選擇團隊核准的版本，再以 `gh api repos/Innoguard-Cyber-Arch/csarc-repo-template/commits/<approved-release-tag> --jq .sha` 取得完整 40 字元 SHA。檢查該 commit 後貼入下列參數，不要直接輸入預留字樣。

```bash
git switch main
git switch -c chore/<issue-number>-update-repo-template
uvx --python 3.14 --from 'git+https://github.com/Innoguard-Cyber-Arch/csarc-repo-template.git@<reviewed-full-commit-sha>' csarc update --to <approved-release-tag> \
  --expected-sha <reviewed-full-commit-sha> --dry-run
uvx --python 3.14 --from 'git+https://github.com/Innoguard-Cyber-Arch/csarc-repo-template.git@<reviewed-full-commit-sha>' csarc update --to <approved-release-tag> \
  --expected-sha <reviewed-full-commit-sha> --yes --non-interactive
```

CLI 會先預覽 Copier smart diff、conflict risk 與 Milestone description migration；確認後才執行 update、`./scripts/verify` 與 repository settings plan。若有衝突，CLI 會列出檔案但不修改 repo；請在目前分支調整衝突內容後重跑。它會更新 `site/` 版型並重建單檔，但不覆寫 `docs/site-content.js` 或 `docs/site-theme.css`，也不會自行 apply settings、push 或開 PR。相依更新一般等待三天；{% if has_typescript %}pnpm 遇到 publisher trust 降級時會停止安裝，先確認套件發布者與 provenance，不要直接停用 policy；{% endif %}OSV 對已公開漏洞立即通知。
{% if enable_template_update_notifications %}
每週 template update workflow 只會建立或更新一張通知 Issue，不會自動套用或合併變更。若 `_src_path` 指向 private GitHub repository，請設定只有來源 repository Contents read 權限的 `CSARC_TEMPLATE_READ_TOKEN`；公開來源不需要這個 secret。
{% endif %}

## 負責人與支援

程式與政策 owner 是 `{{ code_owner }}`；governance workflow 會從 `.github/REVIEWERS` 輪派一位非作者 reviewer。一般問題與疑似資安問題依 [`SECURITY.md`](SECURITY.md) 使用核准的通報管道；預設 GitHub Issue 是公開的，不得張貼 secrets、credentials、personal data 或其他敏感內容。
