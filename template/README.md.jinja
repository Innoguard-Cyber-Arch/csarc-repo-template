# {{ project_name }}

{{ project_description }}

[開啟專案內部網站](docs/index.html)

## 專案概述

本 repo 使用 CSARC 公版，語言 profile 是 `{{ language }}`。請在這裡補上主要使用者、解決的問題、輸出與不處理的範圍；完整團隊流程放在內部網站，不在 README 重複。

## 快速開始

共同需求是 Git、GitHub CLI、uv；{% if language != "python" %}本案另需 Node 24+ 與 pnpm 11。{% endif %}

```bash
git clone <repository-url>
cd {{ project_slug }}
./scripts/verify
```

請在這裡補上產品最短可成功的啟動或呼叫範例。

{% if project_mode == "existing" %}> 既有案導入：Copier 已保留原本的 manifest。先人工合併 dev 相依、版本與工具設定，再重建 lockfile；`coverage_mode=diff` 預設比較 `origin/main`。{% endif %}

## 技術與目錄

| 範圍 | 位置／工具 |
| --- | --- |
| 共用 | `.github/`、`policies/`、`docs/specs/`、`scripts/verify` |
{% if language != "typescript" %}| Python | `src/`、`tests/`、uv、Ruff、mypy、pytest；Python {% if python_support_mode == "latest" %}3.14 only{% else %}{{ python_min_version }}+{% endif %} |
{% endif %}{% if language != "python" %}| TypeScript | `typescript/`、pnpm 11、Biome、strict TypeScript、Vitest；Node 24+ |
{% endif %}| 內部網站 | `docs/index.html` 版型；`docs/site-content.js` 專案內容 |

語言宣告記在 `.csarc/profile.json`；`scripts/detect-language-profile --suggest` 會檢查實際檔案是否一致。

## 開發與驗證

1. 用 Work item 或 `docs/specs/` 寫清楚成果與驗證方式。
2. 在短分支修改；AI 遵守 `AGENTS.md`，不能自行合併。
3. 執行 `./scripts/verify`。
4. PR 標題使用英文 `type(scope): summary`，通過 CI 與必要審查後合併。

目前驗證包含{% if language != "typescript" %} Ruff、mypy、pytest、{{ coverage_threshold }}% coverage、wheel 與相依下界{% endif %}{% if language == "python-typescript" %}，以及{% endif %}{% if language != "python" %} Biome、TypeScript、Vitest {{ coverage_threshold }}% coverage 與 npm tarball{% endif %}，最後再檢查 spec、機密字串與 lockfile。

## 設定與密鑰

未設定專用 GitHub App 時，release-please job 會略過，不影響 CI 或人工 tag。Actions 憑證放 GitHub Secrets／Variables；本機 runtime 才使用未提交的 `.env`。請在這裡列出產品設定的名稱、用途與取得位置，但不要填入真實值。

## 發布與維運

`fix` 升修補版、`feat` 升次版、`!` 升大版。`v*` tag 會產生{% if language != "typescript" %} wheel／sdist{% endif %}{% if language == "python-typescript" %}與{% endif %}{% if language != "python" %} npm tarball{% endif %}、SHA-256 與 SBOM；目前沒有通用部署流程。

若本案是長駐服務，請補上部署入口、健康檢查、復原方法與 runbook。

## 公版更新

```bash
git switch -c chore/update-repo-template
uvx --from copier copier update --trust --vcs-ref <reviewed-full-commit-sha>
./scripts/verify
```

Copier 會更新網站版型，但不覆寫 `docs/site-content.js`。相依更新一般等待三天；OSV 對已公開漏洞立即通知。

## 負責人與支援

程式審查者是 `{{ code_owner }}`。一般問題使用 GitHub Issue；疑似資安事件或敏感資料走組織核准的內部通報管道。
