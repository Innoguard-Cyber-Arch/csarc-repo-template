{% set has_python = language in ["python", "python-typescript"] -%}
{% set has_typescript = language in ["typescript", "python-typescript"] -%}
# {{ project_name }}

{{ project_description }}

[開啟專案內部網站](docs/index.html)

## 專案概述

本 repo 使用 CSARC 公版，語言 profile 是 `{{ language }}`。請在這裡補上主要使用者、解決的問題、輸出與不處理的範圍；完整團隊流程放在內部網站，不在 README 重複。

## 快速開始

共同需求是 Git、GitHub CLI、uv；{% if has_typescript %}本案另需 Node 24+ 與 pnpm 11。{% endif %}

```bash
git clone <repository-url>
cd {{ project_slug }}
{% if branch_strategy == "delivery" %}git switch -c dev/next
git push -u origin dev/next
{% elif branch_strategy == "dev" %}git switch -c dev
git push -u origin dev
{% endif %}
./scripts/verify
./scripts/apply-repository-settings.sh plan
```

請在這裡補上產品最短可成功的啟動或呼叫範例。

{% if project_mode == "existing" %}> 既有案導入：Copier 已保留原本的 manifest。先人工合併 dev 相依、版本與工具設定，再重建 lockfile；`coverage_mode=diff` 預設比較 `origin/main`。{% endif %}
{% if branch_strategy == "delivery" %}> 本案選用 delivery 模式：Milestone 使用 `dev/m<編號>-<簡稱>`，孤立 Issue 使用 `dev/next`，再以 promotion PR 進入 `main`。{% elif branch_strategy == "dev" %}> 本案選用 `dev` 模式，因為有長期共用測試環境。維護者首次啟用時須從 `main` 建立並推送 `dev`。{% else %}> 本案使用 main 模式，短分支通過 PR 後直接合併到 `main`。{% endif %}
{% if branch_strategy == "delivery" %}
`main` 前進後，所有未合併的 delivery／stacked PR 都必須先納入最新 main；delivery-sync workflow 會讓過期 PR fail closed，並列出建立 `sync/main-to-*` PR 的手動指令。預設不自動寫入；只有明確設定 `CSARC_AUTO_SYNC=true`、提供會觸發 PR checks 的 `CSARC_SYNC_TOKEN`，且 branch／PR write probes 都為 allowed 時才自動開 PR，blocked／unknown 一律回到相同手動流程。
{% endif %}

## 技術與目錄

| 範圍 | 位置／工具 |
| --- | --- |
| 共用 | `.github/`、`policies/`、`docs/specs/`、`scripts/verify` |
{% if has_python %}| Python | `src/`、`tests/`、uv、Ruff、mypy、pytest；CI 驗證 {% if python_support_mode == "latest" %}3.14.0 與最新 3.14.x{% else %}{{ python_min_version }}.0，以及 {{ python_min_version }}～3.14 每個 feature release 的最新 patch{% endif %} |
{% endif %}{% if has_typescript %}| TypeScript | `typescript/`、pnpm 11、Biome、strict TypeScript、Vitest；Node 24+ |
{% endif %}| 內部網站 | `docs/index.html` 版型；`docs/site-content.js` 專案內容 |

語言宣告記在 `.csarc/profile.json`；`scripts/detect-language-profile --suggest` 會檢查實際檔案是否一致。
{% if has_python %}Python minimum 選項目前從 3.12 起，未宣告支援 3.11；所選 feature release 的 `.0` 就是 `requires-python` 的實際下界，CI 不會只測較新的 patch。{% endif %}

## 開發與驗證

交付模型是「可選 Story Milestone → 1..N Issues → 各自 PR」。Milestone 只代表可端到端驗收的成果，不是每張 Issue 的必填分類；SDD、一般規劃、使用者 story 或導入盤點都可能成為來源。建立 Issue／Milestone 前，agent 會以具體關鍵字限量搜尋 open／closed Issues，閱讀候選內文、comments 與 linked PR，先向使用者摘要；若已獲准直接建立，則在補充或 Milestone `References` 記錄每項決策是沿用、取代或駁回及理由。description 使用 [`docs/milestone-description.md`](docs/milestone-description.md) 的 Problem、Outcome、Acceptance criteria、Plan、Out of scope、Verification 與 References；英文 H2 是穩定結構，內文使用專案團隊慣用語言。PR 不重複掛入 Milestone。最後一張 open Issue 關閉且所有 acceptance checkbox 已確認時，workflow 才會關閉 Milestone；Issue reopen、open Issue 掛入或 criterion 取消勾選時則重開。

1. 簡單工作先開「開發工作」Issue；標題以 12–80 個英文 ASCII 字元及至少三個詞直接描述成果，例如 `Add dependency policy checks`。Issue 不使用 PR 的 Conventional Commit 格式。
2. 開單者會自動成為負責人，並選一個分類：`bug`、`enhancement`、`documentation` 或 `duplicate`。前三者在 organization 啟用原生 Issue Types 時分別同步成 `Bug`、`Feature`、`Task`；不支援時仍以 label 正常運作。`duplicate` 是結案處置，不是假造的新 Issue Type。
3. {% if branch_strategy == "delivery" %}從 Issue 所屬的 `dev/m<Milestone 編號>-<簡稱>` 建立工作分支；沒有 Milestone 時從 `dev/next` 建立。{% else %}從 `{{ branch_strategy }}` 或最終 PR 回 `{{ branch_strategy }}` 的未合併工作分支建立。{% endif %}分支使用 `type/<issue-number>-short-slug`；若新增需求超出 Issue 的完成條件，另開 Issue 與分支。若後來確認重複，連結 canonical Issue 並用 GitHub 的 Duplicate 原生原因關閉；純 triage 不改檔案，可不開分支與 PR。
4. 執行 `./scripts/verify`；AI 遵守 `AGENTS.md`，一般情況下不能自行合併。
5. PR 指向{% if branch_strategy == "delivery" %}對應 delivery branch{% else %} `{{ branch_strategy }}`{% endif %}或 stack 中的直接上游分支；整條 open PR 鏈必須最終回到該 integration branch。使用 `Closes #<issue-number>` 前，PR 與 referenced Issue 不得留有未勾選 task；PR policy 會直接拒絕，避免局部完成卻自動結案。風險或回退放在選填補充；PR 沒有原生 Issue Type／Issue Form，只使用 Markdown template，並恰選一個 change label：`fix`→`bug`、`docs`→`documentation`，其餘允許的 Conventional Commit type→`enhancement`。
6. 一般情況下，CI 與人工審查都通過才合併；只有下列 Actions 額度例外且 human maintainer 明確授權時，agent 才可依 repo 現行 self-merge 政策合併。{% if branch_strategy == "delivery" %}Milestone 建立時同時建立一張 `promotion` Issue；完成後用 `dev/m* → main` PR 關閉它。孤立工作由 `dev/next → main` 批次 promotion；只有 standalone `fix/*` 加上 `hotfix` label 才能直接 main。{% elif branch_strategy == "dev" %}準備發布時，再用一張審查過的 PR 將 `dev` 升版到 `main`。{% endif %}

多個可寫 agents 平行執行時，一項任務使用一個 branch 與一個獨立 worktree，且只平行處理範圍互不依賴的工作；開始前先辨識目前是否已在 agent 平台管理的 worktree，不強制讓同一 branch 出現在多個 worktrees，也不自行刪除其他工具建立或含未提交變更的 worktree。原生 [`git worktree`](https://git-scm.com/docs/git-worktree) 是可攜基線；[Worktrunk](https://github.com/max-sixty/worktrunk) 可選擇簡化建立、切換與清理，但屬於本機／agent orchestration 輔助工具，不是本模板或生成 repo 的必要相依。worktree 只隔離工作目錄，整合仍以 PR、CI、人工審查及合併後完整驗證為準。

GitHub Actions 看不到 agent host 上的本機 worktrees，因此建立 worktree 的 agent 也負責在 PR 合併後回收：先離開該 worktree 並更新 integration ref，再從另一個 checkout 執行 `./scripts/cleanup-worktrees --apply --worktree <path> origin/{% if branch_strategy == "delivery" %}<delivery-branch>{% else %}{{ branch_strategy }}{% endif %}`。腳本預設只列出候選項目，且只接受有 GitHub merged PR 證據、已進入 integration ref、乾淨且未鎖定的 branch worktree；維護者要清理全 repo 時，先執行不含 `--apply` 的 dry run，再省略 `--worktree` 套用全部候選項目。

`./scripts/verify` 也會跑 Issue／PR 政策正反例，確認合法流程能通過、不合格 Issue 標題與錯誤 PR 標題／分支／Issue 關聯會被拒絕。PR CI 依 docs／fast／full 與週期性供應鏈四層執行，完整矩陣只留給 promotion、hotfix、merge queue 或手動驗證；觸發條件、穩定 required checks、成本估算與實測方式見 [`docs/ci-policy.md`](docs/ci-policy.md)。

{% if branch_strategy in ["dev", "delivery"] %}Promotion 另由穩定的 `promotion` context 封裝候選 source 與 SHA/tree 證據；合併後只核對 `main` tree 與已成功的 `verify`，不重跑完整矩陣。未設定外部環境時明確保留 artifact-only 證據；要啟用 canary，須同時設定 repository variables `CSARC_CANARY_COMMAND`、`CSARC_CANARY_ENVIRONMENT`，敏感值則放在該 environment 的 `CSARC_CANARY_TOKEN` secret。完整三態與失敗邊界見 [`docs/ci-policy.md`](docs/ci-policy.md)。{% endif %}

### Actions 額度耗盡的一次性驗證

只有具帳務用量檢視權限的 human maintainer 已明確確認「當期 GitHub Actions 免費分鐘已用完」時，才可啟用本機 fallback。GitHub 顯示的 runner 未啟動訊息會同時提及付款失敗與 spending limit，本身不足以證明額度耗盡；付款失敗、budget 設定、平台事故、workflow／權限錯誤、原因不明，或任何已開始執行 step 後失敗的 job 都不能使用。

Agent 必須確認乾淨 worktree 的 `HEAD` 等於 PR head SHA，執行 `./scripts/verify` 與所有可忠實本機重現的必要 checks，任何失敗都停止。通過後在 PR 留下標題為 `Actions quota fallback attestation` 的留言，記錄 SHA、受阻 run URL 與 annotation、human quota confirmation、UTC 時間、環境／工具版本、完整命令與結果，以及無法在本機重現的 checks。Human maintainer 必須明確授權該 PR 使用一次性 fallback；新 commit 會使聲明立即失效。若 repo 現行政策允許 author self-merge，agent 才能據此合併，但不得偽造成功 Check Run，也不能取代 release、publishing、deployment approval、secrets、provenance 或 CODEOWNER review。額度重置後須補跑該 commit 的 GitHub checks；無法補跑時另開 Issue 留下缺口。

`./scripts/scan-secrets` 會在已有 commit 時掃描完整可達 Git 歷史，並一律另掃目前工作樹；尚未 `git init` 的新專案仍可掃描工作樹。大型 repo 若已明確接受縮小歷史範圍，可傳入例如 `--log-opts='--since=2026-01-01'`，預設仍掃完整歷史。

{% if enable_codeql | default(false) %}此專案已產生 CodeQL workflow，只分析 {{ "Python 與 TypeScript" if language == "python-typescript" else ("Python" if language == "python" else "TypeScript") }}；它補足跨函式資料流等 SAST 檢查，但仍可能有誤報與漏報，結果需人工 triage。{% else %}此專案未啟用 SAST。Ruff／TypeScript lint 與 OSV 分別處理程式品質和已知相依漏洞，不能替代 SAST；public repo 可重新產生並啟用 `enable_codeql`，private／internal repo 則需先確認 GitHub Code Security 授權，或由專案選用核准的替代工具。{% endif %}

`./scripts/verify-template.sh`、root 專用升版／同步工具與 template release workflows 只屬於中央模板 repo，不會下發到本專案；本專案的本機與 CI 驗證入口都是 `./scripts/verify`。

{% if language == "ci" %}目前驗證 spec、README、JSON、workflow 與機密字串；沒有語言模組，因此不執行語言測試或打包。
{% else %}目前驗證包含{% if has_python %} Ruff、mypy、pytest、{{ coverage_threshold }}% coverage、wheel 與相依下界{% endif %}{% if language == "python-typescript" %}，以及{% endif %}{% if has_typescript %} Biome、TypeScript、Vitest {{ coverage_threshold }}% coverage 與 npm tarball{% endif %}，最後再檢查 spec、機密字串與 lockfile。

Coverage 是找出未測程式碼的訊號，不等於測試品質，也不取代風險分析與人工 review。新案以 global coverage 建立基線；既有案先用 diff coverage 約束本次變更。只有缺陷紀錄、關鍵模組或風險改變時才調整 {{ coverage_threshold }}% 門檻，不要只為提高百分比加入沒有驗證行為的測試。
{% endif %}

## 設定與密鑰

GitHub 從模板建立 repo 或 Copier 導入只會複製檔案，不會複製 repository settings。有管理權且希望啟用較強門禁時，可在推送遠端後執行 `./scripts/apply-repository-settings.sh plan`，確認後執行 `apply`，最後以 `check` 唯讀比對 CODEOWNERS、repository、Actions、政策標籤與有效 Ruleset；這不是 portable release baseline 的必要步驟。可修正差異會失敗，Free private Ruleset、組織政策限制或 PR 的內建 `GITHUB_TOKEN` 看不到管理欄位時，則明確標為 `DEGRADED` 並保留具體差異。需要驗證全部管理欄位時，由管理員在可信任的 checkout 以具 repository Administration read 權限的 `gh` 憑證執行；不要把管理型 token 暴露給未受信任的 PR 程式碼。尚未設定 Git remote 時，明確指定 `GH_REPO=owner/repo`。標籤預設採 additive 更新並保留自訂項目；只有明確傳入 `--prune-labels` 才會刪除政策外標籤。Copier 不在本機建檔階段猜測遠端方案。

| GitHub 方案與可見性 | `apply` 結果 | `check`／PR／CI/CD 行為 |
| --- | --- | --- |
| Free＋public | 透過 REST 套用並啟用 Ruleset | 驗證 `main` 的有效規則；缺少或不符即失敗 |
| Free organization＋private | 套用基本設定，並把期望 Ruleset 保留在 `policies/rulesets.json`；公開 API 無法建立 Ruleset | workflow 從設定名單輪派一位個別 reviewer，並以 `DEGRADED` 警告未強制；team request、紅燈或未核准都不能成為 merge gate |
| Pro 個人帳號＋private | 套用並啟用 Ruleset | 與 Free public 相同 |
| Team／Enterprise organization＋private | 確認 CODEOWNERS team 後套用並啟用 Ruleset | 必要審查、CODEOWNER 與 status checks 成為 merge gate；不符政策時 fail-closed |

`policies/rulesets.json` 是所有方案共用的期望狀態。設定腳本在所有方案都透過 repository teams API 驗證 PR 內設定的 team 存在且對 repo 有 write access；這項檢查與是否能強制 Ruleset 分開，也不依賴預設分支尚未更新的 CODEOWNERS。`.github/workflows/governance-comment.yml` 另在非 draft PR 開啟、重開或轉為 ready 時，從 `.github/REVIEWERS` 排除作者後輪派一位個別 reviewer，並用短效 `GITHUB_TOKEN` 提出 request，不需 PAT 或 GitHub App。Copier 的 `reviewers` 可用逗號設定一到多位候選人。Free private 不支援 team review request 或多位同時 reviewer；GitHub Team 以上才提供這些能力。Free organization private repo 的設定頁雖提供 Ruleset 編輯器，但 GitHub REST 與 GraphQL 對建立／更新（包含 `disabled`）都回覆需升級或公開；因此 `gh` 無法自動預存。管理員可選擇在 Web UI 人工建立 disabled Ruleset，`check` 會將它顯示為 `STAGED`，但仍只代表已儲存，不代表 `main` 受保護。repo 改成 public 或組織升級後，請重新執行 `plan`／`apply`／`check`，由腳本套用 `active` 政策並驗證有效規則。腳本不會自行變更可見性。

{% if language != "ci" %}{% if enable_release_attestations | default(false) %}此專案會為 release distributions 產生 artifact provenance 與 SBOM attestation；這需要 public repo，或支援 artifact attestations 的 GitHub Enterprise Cloud private／internal repo。{% if enable_pypi_publishing | default(false) or enable_npm_publishing | default(false) %}外部 registry 發布會先以 `gh attestation verify` 強制比對 repository、tag ref、artifact digest 與 signer workflow；驗證失敗就不發布。{% else %}目前沒有自動消費或部署路徑，因此只產生 attestation，不宣稱已在消費端強制驗證。{% endif %}{% else %}Artifact attestation 未啟用，適合目前不支援該功能的 private repo；release 仍會附上 `SHA256SUMS` 與 SBOM，可用 checksum 驗證，但沒有 provenance 強制門禁。{% endif %}{% endif %}

release workflow 使用 Actions 內建 `GITHUB_TOKEN`，不需要設定 GitHub App 或 PAT。`csarc init`／`adopt`／`update` 在 GitHub origin 與 API 可讀時顯示 capability preflight；每次 workflow 仍重測 Actions PR、contents、Release 與 dispatch 能力。四項都確認可用時使用 release-please；PR 被禁止或無法確認但其餘交付能力完整時，從最新整合分支 commit 配置版本並交付；否則只驗證並上傳 machine-readable capability artifact。`plan`／`apply`／`check` 是有管理權時啟用較強門禁的選配，不會阻止 runtime 自適應。Actions 憑證放 GitHub Secrets／Variables；本機 runtime 才使用未提交的 `.env`。請在這裡列出產品設定的名稱、用途與取得位置，但不要填入真實值。

同一個唯讀 preflight 也會依 personal／organization owner、repo admin 與 organization owner 狀態，把選配 Renovate App 標成 `available`、`request-owner` 或 `fallback`。`available` 只提供 [安裝入口](https://github.com/apps/renovate/installations/new)，仍須在 GitHub 同意權限並只選本 repo；organization member 即使有 repo admin，仍需 organization owner 核准。未知權限、API 失敗或無 admin 時不猜測，直接保留 Dependabot 與既有 CI/CD。CLI 不會靜默安裝 App，也不要求長效廣域 PAT。新 repo 尚未有 origin 時，先設定 `GH_REPO=owner/repo` 可讓 `csarc init` 檢查預定目標；推送後也能執行 `python scripts/release_policy.py preflight --repo owner/repo` 重查。

## 發布與維運

PR 只顯示升版意圖：`fix(scope)` 是 patch、`feat(scope)` 是 minor、`!` 是 major，其餘是 no-release；不在 PR 階段預約確切版本。進入整合分支後才以實際 commit／tag 順序配置版本，過期或亂序 run 會停止。{% if branch_strategy in ["dev", "delivery"] %}因 repo 採 squash merge，promotion PR 的英文標題必須代表納入 PR 中最高的 SemVer 等級；Milestone 完成時 promotion 一次，`dev/next` 預設每週固定一個 release window 批次 promotion，無 release-worthy 變更就略過，hotfix 才立即發版。{% endif %}release-please 或 direct mode 建立 release 後，只能帶著已核對的 release-source run ID 明確 dispatch 成品 workflow；direct mode 只接受已由人工 PR 寫入版本與 CHANGELOG 的最新整合分支，否則停在 verification-only。任意 `v*` tag push 不會啟動發布；artifact workflow 會核對 promotion evidence 後建置、產生 digest／SBOM／attestation 與驗證 immutable Release，但不重跑完整 runtime CI。{% if language == "ci" %}版本記在 `version.txt`，tag 發布 GitHub Release 的來源封存檔；沒有語言成品、registry 消費路徑或空驗證 job。{% else %}核准的 `v*` tag 會產生{% if has_python %} wheel／sdist{% endif %}{% if language == "python-typescript" %}與{% endif %}{% if has_typescript %} npm tarball{% endif %}、SHA-256、SBOM 與來源 metadata，並附加到對應 GitHub Release。下載後執行 `gh release download <tag>`，再於下載目錄執行 `shasum -a 256 -c SHA256SUMS`；{% if enable_release_attestations | default(false) %}另以 `gh attestation verify <artifact> --repo <owner/repo> --signer-workflow <owner/repo>/.github/workflows/release.yml --source-ref refs/tags/<tag>` 驗證 provenance。{% endif %}{% endif %}目前沒有通用部署流程；批次與追溯規則見 [`docs/ci-policy.md`](docs/ci-policy.md)。

| Runtime 實測政策 | 模式與行為 | 保證、限制與 fallback |
| --- | --- | --- |
| PR、contents、Release、dispatch 都是 `allowed` | **Release PR：**release-please 維護可審查的版本／changelog PR；合併後明確 dispatch 成品 workflow | 保留最強的人類審查與來源 metadata 同步；必要能力漂移就不再選此模式 |
| PR 是 `blocked`／`unknown`，其餘三項都是 `allowed` | **Direct：**只為已版本化且含 CHANGELOG 的最新整合分支配置 tag／draft release | 亂序 run 成為 no-op；缺少版本 commit 時轉為 verification-only |
| contents、Release 或 dispatch 任一不是 `allowed` | **Verification only：**保留驗證與 capability artifact，不建立 tag 或 release | 不把不確定權限當成功；政策恢復後由後續 run 重新計算 |

GitHub Release 是共同基線；外部 registry 只會依本案 profile 與下列明確選項啟用，不是導入必要條件。
{% if enable_pypi_publishing | default(false) %}
PyPI 發布使用 environment `{{ pypi_environment }}` 與 OIDC，不需要 API token。Package owner 必須先在 PyPI 建立 project 或 pending publisher，將 organization、repository、workflow `release.yml` 與 environment 完整對應到本 repo；environment 建議要求人工核准。
{% endif %}{% if enable_npm_publishing | default(false) %}
npm 發布使用 environment `{{ npm_environment }}` 與 OIDC，不需要 `NPM_TOKEN`。既有 package owner 必須先在 npm package Settings 建立 trusted publisher，並精確填入 organization、repository、workflow `release.yml` 與 environment；首次 package ownership 依組織核准流程建立。npm trusted publishing 需要 GitHub-hosted runner、Node 22.14+ 與 npm 11.5.1+，本 workflow 使用 Node 24。
{% endif %}

若本案是長駐服務，請補上部署入口、健康檢查、復原方法與 runbook。

## 公版更新

先用 `gh release list --repo Innoguard-Cyber-Arch/csarc-repo-template` 選擇團隊核准的版本，再以 `gh api repos/Innoguard-Cyber-Arch/csarc-repo-template/commits/<approved-release-tag> --jq .sha` 取得完整 40 字元 SHA。檢查該 commit 後貼入下列參數，不要直接輸入預留字樣。

```bash
git switch {% if branch_strategy == "delivery" %}dev/next{% else %}{{ branch_strategy }}{% endif %}
git switch -c chore/<issue-number>-update-repo-template
uvx --from csarc-repo-cli csarc update --to <approved-release-tag> \
  --expected-sha <reviewed-full-commit-sha> --dry-run
uvx --from csarc-repo-cli csarc update --to <approved-release-tag> \
  --expected-sha <reviewed-full-commit-sha> --yes --non-interactive
```

CLI 會先預覽 Copier smart diff、conflict risk 與 Milestone description migration；確認後才執行 update、`./scripts/verify` 與 repository settings plan。它會更新網站版型，但不覆寫 `docs/site-content.js`，也不會自行 apply settings、push 或開 PR。相依更新一般等待三天；{% if has_typescript %}pnpm 遇到 publisher trust 降級時會停止安裝，先確認套件發布者與 provenance，不要直接停用 policy；{% endif %}OSV 對已公開漏洞立即通知。
{% if enable_template_update_notifications %}
每週 template update workflow 只會建立或更新一張通知 Issue，不會自動套用或合併變更。若 `_src_path` 指向 private GitHub repository，請設定只有來源 repository Contents read 權限的 `CSARC_TEMPLATE_READ_TOKEN`；公開來源不需要這個 secret。
{% endif %}

## 負責人與支援

程式與政策 owner 是 `{{ code_owner }}`；governance workflow 會從 `.github/REVIEWERS` 輪派一位非作者 reviewer。一般問題使用 GitHub Issue；疑似資安事件或敏感資料走組織核准的內部通報管道，詳見 [`SECURITY.md`](SECURITY.md)。
