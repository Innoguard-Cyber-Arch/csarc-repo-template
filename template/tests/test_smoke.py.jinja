from importlib.metadata import version

from {{ package_name }} import __version__


def test_version_matches_installed_distribution() -> None:
    assert __version__ == version("{{ project_slug }}")
